# Журнал решений

Обновлено: 2026-08-31

Этот документ хранит решения, которые нельзя оставлять только в истории чата.

## Принятые решения

| ID | Дата | Статус | Решение | Причина / последствия |
|---|---|---|---|---|
| D-001 | 2026-08-08 | Принято | Продукт обслуживает три магазина: одежда, обувь и сумки | Это исходная бизнес-концепция владельца |
| D-002 | 2026-08-08 | Принято | Есть минимум две роли: владелец и продавец | Владелец видит сеть и управляет продавцами; продажи привязаны к продавцу |
| D-003 | 2026-08-08 | Принято | Текущая версия является frontend demo без backend, DB и API | Позволяет согласовать интерфейс и потоки до production-разработки |
| D-004 | 2026-08-08 | Принято | Demo использует только клиентскую mock-генерацию из текста | Данные никуда не отправляются |
| D-005 | 2026-08-08 | Принято | Визуальный язык: тёмный графитовый, фиолетовые акценты, серьёзный и модный | Постоянное дизайн-направление |
| D-006 | 2026-08-08 | Принято | Production не должен подключаться напрямую к текущему SQLite без отдельного плана миграции | Защита работающего бота и данных |
| D-007 | 2026-08-08 | Принято | Контекст проекта хранится в репозитории, а не только в чатах | Обеспечивает handoff между моделями и агентами |
| D-008 | 2026-08-08 | Принято | Первый продукт — адаптивное web/PWA | Отдельные iOS/Android-приложения сейчас не входят в scope |
| D-009 | 2026-08-08 | Принято | Сначала создаётся приложение, затем Telegram-бот постепенно адаптируется к нему | Бот не блокирует разработку первого приложения |
| D-010 | 2026-08-08 | Принято | Аутентификация будет построена на Supabase Auth с Magic Link | Пользователи входят по одноразовой ссылке из email без пароля |
| D-011 | 2026-08-08 | Принято | Базовая валюта магазина и отчётности — EUR | Курсы других валют позже могут загружаться автоматически из внешнего API |
| D-012 | 2026-08-08 | Принято | Реальный учёт начинается с чистых данных | Товары и продажи существующего бота не мигрируются в первую production-версию |
| D-013 | 2026-08-08 | Принято | Продавец видит закупочную цену, маржу и продажи своего магазина | Продавец не видит другие магазины; владелец видит всю сеть |
| D-014 | 2026-08-08 | Принято | Интерфейс первой версии имеет два языка: English и Türkçe | Русский не входит в production UI первой версии |
| D-015 | 2026-08-08 | Принято | Business week по умолчанию идёт Wednesday–Tuesday | Позже владелец сможет настраивать начало недели |
| D-016 | 2026-08-08 | Принято | Фактическая цена продажи поддерживает EUR, USD, TRY, RUB и GBP | Базовая отчётная валюта остаётся EUR |
| D-017 | 2026-08-08 | Принято | Способы оплаты: cash, card и bank transfer | Необходимость mixed payment ещё не подтверждена |
| D-018 | 2026-08-08 | Принято | Система должна поддерживать перемещения между магазинами | Для первого clothing-only пилота функция может быть подготовлена архитектурно и включена при появлении второго магазина |
| D-019 | 2026-08-08 | Принято | Первый рабочий MVP запускается только для магазина одежды | Обувь и сумки добавляются следующими этапами без смены базовой архитектуры |
| D-020 | 2026-08-08 | Принято | В одежде внутренний code относится к модели; варианты size/color используют тот же code | Для уникальности складской единицы нужен отдельный variant ID; существующие barcodes магазина одежды поддерживаются |
| D-021 | 2026-08-08 | Принято | Карточка одежды содержит модель, бренд, supplier, gender Men/Women/Unisex, цвета, размеры, code, закупочную цену и фотографии | Это обязательный состав каталога первого MVP |
| D-022 | 2026-08-08 | Принято | Hosting: Vercel для Next.js PWA и managed Supabase для Postgres/Auth/Storage | Текущий Contabo VPS остаётся для старого бота до этапа интеграции |
| D-023 | 2026-08-08 | Принято | Одна продажа может содержать несколько товаров | Нужны Sale и SaleLine, а не отдельная sale на каждую позицию |
| D-024 | 2026-08-08 | Принято | В магазине нет скидок как отдельной сущности | Seller видит purchase cost и свободно вводит фактическую цену продажи |
| D-025 | 2026-08-08 | Принято | В первой версии нет возврата денег, поддерживается только обмен товара | Точный расчёт разницы обмена ещё уточняется |
| D-026 | 2026-08-08 | Принято | До автоматического FX API owner вручную вводит дневные курсы | Seller использует утверждённые курсы и не меняет их |
| D-027 | 2026-08-08 | Заменено D-051/D-052 | В Clothing MVP barcode/model code вводится вручную | Product code остаётся основой, barcode стал необязательным, а будущая AI-приёмка начинается с фото накладной. |
| D-028 | 2026-08-08 | Отменено D-051 | Barcode модели одинаковый для всех цветов и размеров | Реальные бирки показывают, что barcode может относиться к конкретному color/size variant или отсутствовать полностью. |
| D-029 | 2026-08-08 | Принято | Одна продажа поддерживает mixed payment | Sale может иметь несколько payment lines, например Cash + Card |
| D-030 | 2026-08-08 | Принято | В первом продукте только две роли: Owner и Seller | Отдельной роли Manager нет |
| D-031 | 2026-08-08 | Принято | Clothing pilot: четыре seller и один owner | Устройства: три iPhone суммарно с owner и двумя sellers, два Android у остальных sellers |
| D-032 | 2026-08-08 | Принято | При обмене на более дешёвый товар разница покупателю не возвращается | При обмене на более дорогой товар покупатель доплачивает разницу |
| D-033 | 2026-08-08 | Принято | Seller может самостоятельно оформить обмен или отменить ошибочную sale | Каждая операция и причина видны Owner в отчётах и audit log |
| D-034 | 2026-08-08 | Принято | Клиентский печатный или электронный чек в Clothing MVP не нужен | Достаточно внутренней записи sale в приложении |
| D-035 | 2026-08-08 | Принято | Закупочная цена поддерживает EUR, USD, TRY, RUB и GBP | Cost сохраняет original currency и EUR snapshot |
| D-036 | 2026-08-08 | Принято | Owner добавляет Seller, указывая email и телефон, после чего система отправляет Magic Link на email | Каждый пользователь имеет отдельную учётную запись; phone хранится в профиле |
| D-037 | 2026-08-08 | Принято | Clothing store называется Zebra Boutique | Название Zebra Woman больше не используется как актуальное имя магазина |
| D-038 | 2026-08-08 | Принято | При выборе товара открывается карточка модели с фотокаруселью и всеми size/color variants | Каталог должен хранить фотографии на уровне модели и объединять варианты по model code |
| D-039 | 2026-08-08 | Принято | Ручная приёмка строится как color → sizes → quantity для каждого size | Отдельная кнопка добавления каждого variant заменена практичной матрицей количеств |
| D-040 | 2026-08-08 | Принято | Seller самостоятельно задаёт личную цель | Цель и прогресс показываются в его кабинете для выбранного периода |
| D-041 | 2026-08-08 | Принято | Приложение поддерживает только Light и Dark themes | Пользователь переключает тему вручную; выбор сохраняется |
| D-042 | 2026-08-08 | Принято | После утверждения visual demo приоритет смещён на Supabase database foundation | Сначала создаются staging-first migrations, RLS и Auth boundary; legacy VPS не подключается |
| D-043 | 2026-08-08 | Принято | В Supabase включаем Data API и automatic RLS, но отключаем automatic table exposure | Доступ к каждой таблице выдаётся миграцией минимально и контролируется RLS; новые таблицы не становятся доступными по умолчанию |
| D-044 | 2026-08-08 | Принято | Вход доступен только уже приглашённым email и требует active store membership | Magic Link не создаёт неизвестного пользователя; middleware проверяет сессию и назначение до открытия рабочей области |
| D-045 | 2026-08-08 | Принято | Production UI имеет только English и Turkish, English — базовый язык | Выбор языка доступен на desktop/mobile, хранится в profile preferences; русский UI исключается |
| D-046 | 2026-08-10 | Принято | Mixed payment — явный opt-in режим оплаты, а не режим добавления товаров | Обычная sale показывает один выбранный Cash/Card/Bank transfer payment; чекбокс `Mixed payment` раскрывает дополнительные payment lines. |
| D-047 | 2026-08-10 | Отменено D-048 | Mixed payment без ручной sale price доступен только для одного товара | Ограничение не соответствует реальной продаже нескольких товаров за одну согласованную сумму. |
| D-048 | 2026-08-10 | Принято | Цена товаров и оплата продажи — независимые понятия; multi-item sale поддерживает `Per-item price` и `Total sale price`, а single/mixed payment относится ко всей продаже | В режиме общей цены продавец добавляет товары без искусственного распределения выручки, затем вводит одну или несколько фактически полученных сумм. Sale хранит общий EUR revenue, исходные payment lines и общую margin; фактическая выручка по отдельному товару не выдумывается. |
| D-049 | 2026-08-10 | Принято | Price Type выбирается первым обязательным шагом New Sale | До выбора `Per-item price` или `Total sale price` товарные поля скрыты. Затем идут code → color → size; Mixed payment показывается перед вводом цены соответствующего режима. |
| D-050 | 2026-08-10 | Принято | Для одной позиции в `Per-item price` Mixed Payment автоматически определяет EUR-цену | Сумма payment lines конвертируется по дневным FX-курсам, ручные поля цены скрываются. Для нескольких товаров с одной общей суммой используется `Total sale price`; multi-item `Per-item price` сохраняет явные цены позиций. |
| D-051 | 2026-08-11 | Принято | Clothing inventory является code-first: обязательный `Product code / Ürün Kodu` и внутренние UUID определяют модель/variant; barcode необязателен, не блокирует операции и может быть добавлен позже | Barcode пока не используется как обязательный рабочий сценарий. Если он есть, он хранится как дополнительный идентификатор модели или конкретного color/size variant; QR нельзя считать товарным barcode без проверки payload. Shoes/Bags позже могут иметь barcode-first UI, сохраняя ту же внутреннюю модель. |
| D-052 | 2026-08-11 | Принято | Основной будущий сценарий приёмки начинается с фото/PDF накладной: система распознаёт документ и автоматически заполняет receipt draft, а человек проверяет и явно подтверждает складское поступление | AI/OCR не изменяет stock напрямую. Черновик содержит supplier, номер/дату/валюту накладной, строки `Ürün Kodu`, description, expected quantity, unit cost и total; затем строки сопоставляются с catalog и раскладываются по color/size quantities. |
| D-053 | 2026-08-11 | Принято | В New Sale продавец может открыть камеру или загрузить одну/несколько фотографий товарных этикеток; AI формирует неподтверждённый cart draft по brand, product/model code, color и size | Product code остаётся главным ключом, barcode/QR optional. AI не создаёт товар, не выбирает фактическую цену с бирки, не списывает stock и не подтверждает sale. Unknown/ambiguous/out-of-stock labels требуют ручного решения; продавец проверяет cart и завершает обычный pricing/payment flow. |
| D-054 | 2026-08-14 | Принято | Inventory показывает Seller и Owner компактное первое фото модели и purchase cost в исходной валюте; Product Card не показывает purchase cost, а показывает только sell price. Sell price — только ориентир `purchase cost × 3`, не хранится и не фиксирует фактическую цену Sale. Seller видит aggregate текущего магазина за today/week и только свои totals за today/week/month/year/all time | Фото остаются private и загружаются существующими signed URLs. Store aggregate не раскрывает строки или личные результаты других Seller; доступ проверяется server-side. |
| D-055 | 2026-08-14 | Принято | До завершения Clothing Pilot действует feature freeze: release path включает только стабилизацию RC, staging, operations, production и pilot; AI receipt, Telegram, AI labels и multi-store остаются post-launch backlog | Это сохраняет ранее принятые D-009/D-019, исключает повторную реализацию готового MVP и снижает риск перед первым реальным запуском. Старый roadmap заменён линейным launch plan TASK-142—TASK-151 с переиспользованием существующих pending tasks. |
| D-056 | 2026-08-14 | Принято | Между чатами `PROJECT_STATUS.md` является единственным текущим указателем, а работа запускается явной командой Owner `Выполни TASK-NNN`; после completion агент переключает указатель на одну следующую TASK и останавливается | Новый агент не зависит от истории чата, не повторяет завершённую работу и не начинает следующий шаг автоматически. `ROADMAP.md` хранит общую последовательность и статусы launch steps. |
| D-057 | 2026-08-15 | Принято | Для TASK-118 staging fixture `TASK021-FX-BOUNDARY` не удаляется, а архивируется через Owner-only flow; 13 точно audited legacy variants нормализуются до canonical English storage values | Fixture имеет receipt/sale history и должен сохранять ledger. Archive имеет Owner audit и restore rollback. Нормализация выполнена только после audit на collisions, транзакционной migration и Owner approval; UI локализует значения в EN/TR. |
| D-058 | 2026-08-15 | Принято | Для TASK-147 четыре обнаруженные test sale без captured payment отменяются только через Owner audited cancellation с причиной `TASK-147 staging cleanup`; 11 существующих `manual_correction` уровня `review` признаются ожидаемыми staging fixtures | Четыре sales не удаляются: cancellation сохраняет immutable history, stock reversal и audit. Manual-correction rows не являются ledger error и не переписываются; они остаются видимыми Owner как historical audit evidence и не блокируют staging exit. |
| D-059 | 2026-08-15 | Принято | TASK-081 использует Plan B: GitHub Actions ежедневно создаёт encrypted logical PostgreSQL dump и `product-images` mirror, затем сохраняет его на Owner VPS на 14 дней | VPS получает только encrypted archive через отдельный non-privileged `zebra-backup` user; legacy bot, SQLite и `/root/zebra` не входят в backup path. Private SSH/Storage/DB credentials хранятся только в GitHub Secrets, а `age` private restore key остаётся вне GitHub/VPS. |
| D-060 | 2026-08-16 | Принято | Repository `coolox/zebra-inventory-sales` остаётся PUBLIC на время Clothing Pilot | Owner подтвердил открытость кода после аудита видимости. Публично доступны schema, RLS-политики, устройство backup-контура и операционная документация; это принято сознательно. Безопасность опирается исключительно на secret store: значения secrets не в git, а `ci.yml` использует `pull_request`, не `pull_request_target`, поэтому fork PR не получают secrets. Следствие как постоянное требование: endpoint, host keys, ключи и пароли никогда не записываются в файлы репозитория, включая docs. |
| D-061 | 2026-08-16 | Принято, уточнено на TASK-082 | Restore из backup archive всегда начинается с применения цепочки миграций, а не только с загрузки dump | `supabase db dump` не выгружает DDL управляемой схемы `auth`, поэтому в archive отсутствует триггер `on_auth_user_created`, хотя данные `auth.users` присутствуют. Проверено на TASK-082: в дампе его нет, а после применения миграций он создаётся. Restore только из archive молча потеряет автосоздание профилей. Первоначальная формулировка приписывала то же самое event trigger `rls_auto_enable` — это было неверно: см. D-062. |
| D-062 | 2026-08-16 | Принято, уточнено проверкой | Единственное расхождение staging с миграциями — незаряженная функция `rls_auto_enable`; `statement_timeout` расхождением **не является** | Первоначально запись утверждала, что `statement_timeout` `3s`/`8s` существует только в `roles.sql` и потому не попадёт в production. Проверка на чистой базе после миграций опровергла это: `anon` уже имеет `3s`, `authenticated` — `8s`. Это платформенные значения Supabase, а `roles.sql` их лишь дублирует. Миграция для timeouts не нужна и не создавалась. Реальное расхождение одно: `rls_auto_enable` — event-trigger функция (`SECURITY DEFINER`, `search_path=pg_catalog`), включающая RLS на любой новой таблице `public`. Она есть в дампе staging, отсутствует в репозитории, и event trigger для неё не зарегистрирован нигде. То есть это незавершённая страховка, а не мусор; вызвать её напрямую нельзя. Все 21 таблица и так имеют RLS явно из миграций, поэтому её отсутствие ничего не ослабляет. Решение о снятии со staging либо о полноценной регистрации принимается отдельно и не входит в RC пилота. |

| D-063 | 2026-08-16 | Принято | Owner принимает RPO до 24 часов для Clothing Pilot: backup выполняется раз в сутки по расписанию `20 1 * * *` UTC | При потере базы теряются продажи, внесённые после последнего успешного прогона. Для пилота одного магазина это сознательный компромисс вместо платного Supabase Pro с point-in-time recovery. Следствие: восстановление за пропущенный период выполняется вручную через audited flow, а не правкой ledger; при росте оборота решение пересматривается. Восстановление проверено на TASK-082, RTO локально измеряется минутами. |
| D-064 | 2026-08-16 | Принято | Production Supabase размещается в AWS `eu-central-1`; production Supabase и Vercel создаются отдельными пустыми projects до запуска приложения | Owner подтвердил регион. Production остаётся изолированным от staging; credentials и endpoints существуют только в hosted secret management и не записываются в repository. До TASK-150 нет Git deployment, pilot users и реальных данных. |
| D-065 | 2026-08-16 | Принято | До покупки собственного домена production Auth использует единственный managed Vercel HTTPS origin | Это временная бесплатная граница для Site URL и redirect allowlist. При переходе на собственный domain owner добавляет новый exact HTTPS origin, проверяет Magic Link и только затем удаляет старый; endpoint value не записывается в repository. |
| D-066 | 2026-08-16 | Принято | Для малого Clothing Pilot custom SMTP временно отправляет через Owner-controlled Gmail account с Google App Password и sender name `Zebra Retail` | Credentials существуют только в Supabase dashboard. Provider предупреждает о personal, а не transactional, sending; это принято только для малой группы пилота. Перед расширением требуется verified transactional sender/domain и повторная delivery acceptance. |
| D-067 | 2026-08-20 | Принято | Исправление Product code меняет только current catalog identity; уже записанные receipt/sale/ledger rows и их snapshots не переписываются | Model/variant UUID остаются общей связью истории, а audit `product_model.code_updated` хранит old/new code и actor. Старый code не становится скрытым alias. |
| D-068 | 2026-08-26 | Принято | Для продажи, внесённой после ухода клиента через серию фотографий этикеток, временем продажи является server-confirmed момент успешного финального сохранения | Seller не вводит и не редактирует отдельное время фактического расчёта/обслуживания; reports, business date и audit используют обычный подтверждённый sale timestamp. |
| D-069 | 2026-08-26 | Принято | После Clothing Pilot дневные FX rates должны автоматически загружаться server-side из утверждённого источника | TASK-206 выбирает provider/rate basis и добавляет source metadata, validation, stale/carry-forward policy и monitoring. Owner manual audited fallback сохраняется; Seller rates не меняет; historical FX snapshots не пересчитываются. D-026 действует до завершения TASK-206. |
| D-070 | 2026-08-31 | Принято | Для автоматических FX используется TCMB daily XML, единый basis — `Döviz Satış` (`ForexSelling`), первая итерация покрывает EUR/USD/TRY | TCMB values нормализуются к EUR base; источник, source date и status обязательны в следующих slices. Не более трёх Istanbul business days carry-forward, без автоматического provider fallback. Owner manual audited override сохраняется; historical snapshots не пересчитываются. |

## Предварительные решения

| ID | Дата | Статус | Решение | Что нужно подтвердить |
|---|---|---|---|---|
| D-101 | 2026-08-08 | Предварительно | Склад строится на журнале движений | Утвердить после выбора backend/DB |
| D-102 | 2026-08-08 | Предварительно | Web и Telegram используют один application backend | Решить, сохраняется ли Telegram-бот |
| D-103 | 2026-08-08 | Заменено D-052 | AI/OCR формирует только подтверждаемый черновик приёмки | Требование подтверждено и уточнено в D-052. |
| D-104 | 2026-08-08 | Заменено D-051 | Товар разделяется на модель и варианты размер/цвет | Code-first identity и необязательные barcodes подтверждены в D-051. |
| D-105 | 2026-08-08 | Заменено D-011 | Базовая отчётная валюта — EUR | Решение подтверждено владельцем и перенесено в принятое D-011 |
| D-106 | 2026-08-08 | Заменено D-015 | Business week начинается в среду, как в текущем боте | Подтверждено владельцем и перенесено в принятое решение D-015 |
| D-107 | 2026-08-08 | Заменено D-022 | Рекомендуемая инфраструктура: managed Supabase для Postgres/Auth/Storage и Vercel для Next.js PWA | Владелец подтвердил рекомендацию в D-022 |

## Ожидают решения

На текущем этапе блокирующих продуктовых решений нет. Новые вопросы добавляются сюда по мере visual review и реализации.

## Как добавлять решение

Запись должна содержать дату, статус, чёткое решение и последствия. Если новое решение отменяет старое, старое не удалять: изменить его статус на `Отменено` и сослаться на новый ID.
